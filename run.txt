// FRONTEND
cd frontend
npm install
npm run dev

// BACKEND (FIRST TIME)
cd backend
python -m venv venv
venv\Scripts\activate
python -m pip install --upgrade pip setuptools wheel
python -m pip install -r requirements.txt

// BACKEND (RUN SERVER EVERY TIME)
cd backend
venv\Scripts\activate
python -m uvicorn app:app --host 0.0.0.0 --port 8000
