experiments:
  experiment_id: "baseline_llm_001"
  description: "Direct unconstrained LLM generation baseline without evidence pipeline"
  retrieval:
    type: "none"
  generation:
    unconstrained: true
    citations: "none"
